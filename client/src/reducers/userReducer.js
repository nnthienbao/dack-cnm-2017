const userReducer = (state = {}, action) => {
    switch (action.type) {
        case 'REGISTER':
            break;
        default:
            break;
    }
    return state;
}

export default userReducer;