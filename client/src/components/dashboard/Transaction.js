import React from 'react';

const Transaction = (props) => (
    <header className="page-header">
        <div className="container-fluid">
            <h2 className="no-margin-bottom">Transaction</h2>
        </div>
    </header>
)

export default Transaction;